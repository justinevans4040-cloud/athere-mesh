#!/usr/bin/env node
/**
 * Backward-compatible entry: registers every active Tailscale peer (not only phones).
 * Prefer: node scripts/mesh-register-tailscale-fleet.js
 */
import './mesh-register-tailscale-fleet.js';
